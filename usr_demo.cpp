#include<stdio.h>
#include<ctype.h>
int main()
{
	int no;
	int v;
	char nm[20]=typeof(no)
	printf("enter number ");
	scanf("%d",&no);
	if(nm==typeof(v))
	   printf("%d",no);
	else
	  printf("error");
	return 0;
}
